#include<stdio.h>
#include<stdlib.h>
int main(int argc, char const *argv[])
{
	int n,sum;
	printf("enter\n");
	scanf("%d",&n);
	char(n);
	int  s=strlen(n);

for(int i=s;i>=s;i--)
{
 sum=i;
}printf("%d",sum );
return 0;
}